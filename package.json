{
  "private": true,
  "scripts": {
    "bump": "changeset && changeset version",
    "release": "turbo telemetry disable && turbo build lint test && changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "turbo": "1.11.3"
  },
  "packageManager": "npm@10.2.4",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ]
}
